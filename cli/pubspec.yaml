name: kinetic_ui
description: Flutter UI component CLI — inspired by shadcn/ui
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.4.0
  http: ^1.2.0
  path: ^1.9.0
  yaml: ^3.1.0
  yaml_edit: ^2.2.0
  crypto: ^3.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.0
  http_mock_adapter: ^0.6.0

executables:
  kinetic: kinetic
